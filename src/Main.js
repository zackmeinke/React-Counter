import React, { Component } from 'react';
import Counter from './Counter.js';

class Main extends Component {
    render() {
        return (
            <div>
                <Counter />
            </div>
        );
    }
}

export default Main;